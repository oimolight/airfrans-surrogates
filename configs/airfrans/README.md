# AirfRANS experiment configs

This directory contains approved experiment input configurations. At runtime, defaults and overrides are resolved and saved to `resolved_config.json` in the run directory.

Configuration values are fixed before an experiment. Do not substitute an unapproved model, data split, or compute budget.
