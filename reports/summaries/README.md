# Experiment summaries

This directory contains compact comparisons across runs, checkpoint locks, profile data and representative predictions. Every reported measurement must resolve to a run ID or a machine-readable artifact. Values absent from run evidence are not filled in retrospectively.

The representative prediction NPZ is intentionally versioned because it supports the committed field figures. Model checkpoints are not versioned.
